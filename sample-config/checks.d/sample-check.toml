# sample check - this will simply execute the bash `true` and return success, running every 1 minute
name = "sample"
description = "sample check"
command = "echo -n 'foo' | expect-output.sh foo"
interval = "1m"
timeout = "5s"